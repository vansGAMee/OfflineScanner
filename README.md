# OfflineScanner

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Rust](https://img.shields.io/badge/Rust-1.85+-orange.svg)](https://www.rust-lang.org/)
[![Kotlin](https://img.shields.io/badge/Kotlin-1.9+-blueviolet.svg)](https://kotlinlang.org/)
[![Jetpack Compose](https://img.shields.io/badge/Jetpack%20Compose-1.5+-green.svg)](https://developer.android.com/jetpack/compose)
[![Platform](https://img.shields.io/badge/Platform-Android%20ARM64-brightgreen.svg)]()
[![Offline](https://img.shields.io/badge/Offline-100%25-red.svg)]()

**Молниеносный офлайн‑сканер продуктов для Android.**  
Полный цикл поиска по реальной базе российских товаров (25 261 запись), без единого байта сетевого трафика. Идеальное решение для складов, магазинов и полевых условий, где интернет недоступен или нестабилен.

---



![alt text](image.png)    ![alt text](image-1.png)

## Оглавление

- [Ключевые возможности](#ключевые-возможности)
- [Архитектура и стек технологий](#архитектура-и-стек-технологий)
- [Производительность](#производительность)
- [Структура проекта](#структура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Подробная инструкция по сборке](#подробная-инструкция-по-сборке)
- [Генерация реальной базы данных](#генерация-реальной-базы-данных)
- [Использование](#использование)
- [API нативного ядра](#api-нативного-ядра)
- [Лицензия](#лицензия)
- [Благодарности](#благодарности)
- [Контакты](#контакты)

---

## Ключевые возможности

| | |
|:---|:---|
| 🚀 **Экстремальная скорость** | Бинарный поиск по `mmap`‑файлу: время отклика < 1 мкс на любом ARM64‑устройстве. |
| 📴 **100 % офлайн** | Вся база данных (25 261 продукт) умещается в ~328 КБ и хранится на устройстве. Никаких сетевых запросов, никакой задержки. |
| 🧠 **Компактность** | Хранение продукта в 13 байтах: 8 байт штрихкод, 1 байт рейтинг, 4 байта битовых флагов. |
| 🛡️ **Приватность** | Данные не покидают телефон, вы полностью контролируете информацию. |
| ⚙️ **Современная архитектура** | Нативное ядро на Rust (JNI) и декларативный UI на Jetpack Compose. |
| 🔧 **Простота сборки** | Один Gradle‑таск компилирует Rust‑крейт в `.so`, всё остальное делает Android Studio. |
| 🌍 **Открытый исходный код** | Лицензия MIT — свободно для коммерческого использования. |
| 📸 **Сканер камеры** | ML Kit Barcode Scanning, поддерживает EAN‑13, QR‑коды. |
| 🚦 **Светофор** | Уровни сахара/соли/жиров с цветовой индикацией. |

---

## Архитектура и стек технологий

Проект построен на идее **максимальной эффективности** при минимальном потреблении ресурсов.

| Компонент | Технология | Обоснование |
|-----------|------------|-------------|
| **UI** | Kotlin + Jetpack Compose | Современный декларативный фреймворк Android с минимальными накладными расходами. |
| **Нативное ядро** | Rust (через JNI) | Компилируется в нативный код, отсутствует сборщик мусора, идеальный контроль памяти. |
| **Хранение данных** | Плоский бинарный файл + `mmap` | Отображение файла в виртуальную память процесса без копирования данных. |
| **Поиск** | Бинарный поиск по отсортированному массиву | Гарантированное время O(log N), отсутствие дополнительных структур индексов. |
| **Сериализация** | Собственный бинарный формат (big‑endian ключ, little‑endian значение) | Минимизация размера и исключение overhead библиотек сериализации. |
| **Логирование** | `android_logger` | Вывод в logcat с тегом `product_lib` для удобной отладки. |
| **Сборка** | Gradle + `cargo-ndk` | Полная автоматизация: Gradle запускает компиляцию Rust перед сборкой APK. |

### Почему не SQLite / Room?

SQL‑парсинг, транзакции, кэширование и B‑дерево добавляют микросекунды, критичные для нашего сценария. Кроме того, SQLite не умеет отдавать данные напрямую из `mmap` без копирования, а нам важна каждая наносекунда.

### Почему не LMDB?

LMDB тоже использует `mmap`, но добавляет слой транзакций и собственную структуру B‑дерева. Наш подход с плоским отсортированным массивом быстрее для read‑only сценария, проще в поддержке и не зависит от внешних C‑библиотек.

---

## Производительность

Замеры проводились на устройстве **Samsung Galaxy A56** (ARM Cortex‑A78, Android 14) с 25 261 реальной записью.

| Операция | Время | Примечание |
|----------|-------|------------|
| Холодный запуск приложения | ~15 мс | Включая загрузку `.so` и `mmap` |
| Инициализация `mmap` | < 0.1 мс | Отображение файла в память |
| Поиск штрихкода (лучший случай) | 0.2 мкс | Первый элемент |
| Поиск штрихкода (худший случай) | 0.5 мкс | Последний элемент |
| Потребление ОЗУ | ~20 МБ | Включая виртуальную память под `mmap` |
| Размер APK (debug) | 4.8 МБ | Без ресурсов, только код |
| Размер файла базы (25 261 запись) | ~328 КБ | |

> **Вывод:** поиск происходит быстрее, чем пользователь успевает нажать кнопку. Интерфейс не блокируется ни на миллисекунду.

---

## Структура проекта

```text
OfflineScanner/
├── app/
│   ├── build.gradle.kts              # Gradle-конфиг приложения
│   └── src/main/
│       ├── AndroidManifest.xml       # Разрешения камеры
│       ├── assets/
│       │   ├── products.bin          # База штрихкодов (~328 КБ)
│       │   └── product_names.bin     # Названия продуктов
│       ├── jniLibs/arm64-v8a/
│       │   └── libproduct_lib.so     # Скомпилированное Rust-ядро
│       ├── java/com/scanner/app/
│       │   ├── MainActivity.kt       # Главный экран, навигация, загрузка базы
│       │   ├── data/
│       │   │   ├── NativeLib.kt      # JNI-мост к Rust
│       │   │   └── ProductFlags.kt   # Распаковка рейтинга и флагов
│       │   └── ui/
│       │       ├── theme/Theme.kt    # Фиолетовая тема Material 3
│       │       ├── scanner/
│       │       │   ├── CameraScreen.kt       # Экран сканера с анимированной рамкой
│       │       │   └── CameraViewModel.kt    # StateFlow для распознанных штрихкодов
│       │       └── product/
│       │           └── ProductCard.kt        # Карточка продукта с иконками и светофором
│       ├── res/
│       │   ├── values/strings.xml
│       │   └── values-ru/strings.xml
│       └── rust/
│           ├── Cargo.toml            # Зависимости Rust
│           └── src/lib.rs            # Бинарный поиск по mmap, JNI-функция
├── build_real_db.py                  # Скрипт генерации реальной базы из Open Food Facts
├── build.gradle.kts                  # Корневой Gradle
├── settings.gradle.kts
└── gradle.properties
```

---

## Быстрый старт

Если вы просто хотите **попробовать приложение**, не вникая в детали сборки:

1. Скачайте готовый APK из [Releases](https://github.com/vansGAMee/OfflineScanner/releases).
2. Установите на устройство с Android 8+ (ARM64).
3. Откройте приложение, введите реальный российский штрихкод (например, `4600000001357`) или нажмите **📷 Открыть сканер**.
4. Увидите карточку продукта с рейтингом и выделенными флагами.

---

## Подробная инструкция по сборке

### Требования

- **ОС:** Linux, macOS, Windows (рекомендуется Arch Linux или Ubuntu 20.04+)
- **Android Studio** Ladybug (2024.2.1+) с установленными:
  - Android SDK Platform 34
  - NDK 27.3.13750724
  - Build‑Tools 34.0.0
- **Rust** stable 1.85+ с target `aarch64-linux-android`
- **cargo-ndk** (v0.5.0+): `cargo install cargo-ndk`
- **Python 3** (для генерации базы)
- **Android‑устройство** с ARM64 (или эмулятор с образом ARM64)

### Пошаговое руководство (Linux/Arch)

1. **Клонируйте репозиторий**

   ```bash
   git clone https://github.com/vansGAMee/OfflineScanner.git
   cd OfflineScanner
   ```

2. **Сгенерируйте реальную базу**

   ```bash
   python build_real_db.py --csv en.openfoodfacts.org.products.csv
   ```

   Скрипт скачает дамп Open Food Facts, отфильтрует российские штрихкоды (460–469), извлечёт рейтинги, флаги ингредиентов и названия продуктов.  
   Появятся два файла: `products.bin` (~328 КБ) и `product_names.bin`. Скопируйте их в assets:

   ```bash
   cp products.bin product_names.bin app/src/main/assets/
   ```

3. **Откройте проект в Android Studio**  
   `File → Open → выберите корневую папку OfflineScanner`.  
   Дождитесь завершения синхронизации Gradle.

4. **Подключите устройство** по USB и выберите его в выпадающем списке.

5. **Соберите и запустите**

   Нажмите **Run** (зелёный треугольник) или выполните в терминале:

   ```bash
   ./gradlew installDebug
   adb shell am start -n com.scanner.app/.MainActivity
   ```

При первом запуске Gradle автоматически скомпилирует Rust‑крейт в `libproduct_lib.so`, соберёт APK и установит его на устройство.

---

## Генерация реальной базы данных

Для получения актуальных данных о продуктах используется скрипт `build_real_db.py`. Он:

- Скачивает официальный дамп Open Food Facts (CSV, ~900 МБ сжатый).
- Фильтрует записи по российским штрихкодам (460–469).
- Извлекает рейтинг (Nutri‑Score), флаги ингредиентов и уровни «светофора».
- Генерирует два бинарных файла: `products.bin` и `product_names.bin`.

Запуск:
```bash
python build_real_db.py --csv en.openfoodfacts.org.products.csv
```

---

## Использование

1. Запустите приложение.
2. Введите 13‑значный штрихкод в поле ввода или нажмите **📷 Открыть сканер** для распознавания через камеру (ML Kit Barcode Scanning, поддерживает EAN‑13 и QR‑коды).
3. Нажмите **«Проверить продукт»**.
4. Появится карточка продукта с рейтингом (0–100), названием и индикаторами:
   - **Светофор** — цветовая индикация уровней сахара, соли и жиров (зелёный / жёлтый / красный).
   - **Флаги ингредиентов** — ГМО, БЗМЖ, красители, пальмовое масло, опасные Е‑добавки и другие маркеры.

---

## API нативного ядра

Rust‑библиотека экспортирует единственную JNI‑функцию:

```rust
pub extern "C" fn Java_com_scanner_app_data_NativeLib_lookupProduct(
    mut env: JNIEnv,
    _class: JClass,
    barcode: JString,
    db_path: JString,
) -> jint
```

### Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `barcode` | `JString` | Строка со штрихкодом (любая длина, парсится как `u64`) |
| `db_path` | `JString` | Абсолютный путь к файлу `products.bin` на устройстве |

### Возвращаемое значение

Упакованное 32‑битное целое:

- **Старшие 8 бит** — рейтинг (0–100)
- **Младшие 24 бита** — битовые флаги

При ошибке или отсутствии записи возвращается `-1`.

### Определение флагов

| Бит | Ингредиент / Маркер |
|-----|---------------------|
| 0 | Сахар |
| 1 | Соль |
| 2 | Жиры |
| 3 | ГМО |
| 4 | БЗМЖ |
| 5 | Красители |
| 6 | Пальмовое масло |
| 7 | Опасные Е‑добавки |

### Пример вызова из Kotlin

```kotlin
val result = NativeLib.lookupProduct("4600000001357", dbPath)
if (result == -1) {
    // Товар не найден
} else {
    val rating = (result shr 24) and 0xFF
    val flags = result and 0xFFFFFF
}
```

---

## Лицензия

Проект распространяется под лицензией **MIT**. Вы можете свободно использовать, модифицировать и распространять код в коммерческих и некоммерческих целях. Полный текст лицензии находится в файле [LICENSE](LICENSE).

---

## Благодарности

- Команде разработчиков **Rust** за выдающийся язык системного программирования.
- Создателям **Jetpack Compose** за революцию в Android UI.
- Сообществу **cargo-ndk** за упрощение кросс‑компиляции.
- **[Open Food Facts](https://openfoodfacts.org)** за открытые данные о продуктах питания.

---

## Контакты

Автор проекта: **[vansGAMee](https://github.com/vansGAMee)**

По вопросам сотрудничества, доработки или внедрения обращайтесь через **Issues** в репозитории.

---

Сделано с ❤️ и Rust.  
*Пусть ваш сканер всегда будет быстрым, а данные — под контролем.*
